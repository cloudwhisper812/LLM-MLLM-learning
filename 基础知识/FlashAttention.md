## flash attention

### 传统 attention 的问题
GPU 计算很快，bottleneck 在 memory bandwidth，IO issue。SRAM 小，存不下 N x N 的大矩阵。Q × K^T 得到 S 存到 HBM（显存），softmax 后得到 P 存到显存。P × V 得到 O，再写到显存。

### FlashAttention
- 切块，把 Q, K, V 切成很多小块，每次只把一小块搬进 SRAM。在 SRAM 里算完这一小块的 attention。避免了中间结果在 HBM 上的读写。
- 引入的两个新问题如何解决：
  - 向前传播没有存中间矩阵 S和P，反向传播要用到怎么办，再算一遍！
- softmax 需要整行求和的统计量，解决方法 online softmax：它不需要等整行算完。它维护一个局部最大值和局部和。算一块，更新一下局部统计量；再算下一块，修正一下之前的统计量。
> online softmax 还挺有意思，说一下个人理解：前面的 block 可以先计算 o，只存分子部分（加权和），以及到目前为止的 softmax 分母局部总和，以及最大值。当计算到新的 block，发现有了新的最大值，更新最大值，修正过去的 o（分子部分）的尺度，更新分母总和（有了新的局部），这样到最后一个 block 后，我们就有了修正正确的每个 o 的分子，也有了正确的分母总和，一除就得到了正确尺度的每个 o。


可以看一下这一个例子讲解：
我还是没太理解flash attention，你用tensor size的level，来给我讲q，k，v，然后如何切块，如何得到o

Gemini said
太棒了！抛开那些抽象的“分母、分子、最大值”，直接下沉到 Tensor Size (张量维度) 和 For Loop (循环逻辑) 的层面，这才是算法工程师之间最底层的共同语言。

我们直接拿目前工业界最普及的 FlashAttention-2 的底层切割逻辑（Tiling）来推演。它的核心设计是：外层循环切分 Q，内层循环切分 K 和 V。

为了让你有最直观的体感，我们先设定一组真实的张量维度：

序列长度 (Sequence Length): N=4096

注意力头维度 (Head Dimension): d=128

HBM (慢速显存) 中的完整输入张量: * Q∈R 
4096×128
 

K∈R 
4096×128
 

V∈R 
4096×128
 

最终要算出的输出 O∈R 
4096×128
 

第一步：根据 SRAM 大小确定“切块尺寸” (Block Size)
GPU 的 SRAM 极小，装不下完整的矩阵。所以我们要在行方向（Sequence Length 维度）上把大张量切成小块：

我们设定查询块大小 (Row Block Size): B 
r
​
 =64。也就是把 Q 和 O 切成一小块一小块的，每块包含 64 个 Token。

设定键值块大小 (Col Block Size): B 
c
​
 =64。把 K 和 V 也切块，每块包含 64 个 Token。

所以，在接下来的计算中：

一个小块 Q 
block
​
  的维度是 64×128

一个小块 K 
block
​
  的维度是 64×128

一个小块 V 
block
​
  的维度是 64×128

第二步：双重循环计算流 (The For-Loops)
准备好，我们要开始把数据在 HBM 和 SRAM 之间搬运了，请死死盯住每一步计算的 Tensor Size。

🔹 外层循环 (Outer Loop): 遍历 Q 的分块
我们一共有 4096/64=64 个 Q 的分块。
假设我们现在拿到了第 1 个分块 Q 
1
​
 。

加载: 从慢速 HBM 把 Q 
1
​
  搬到极速 SRAM 里。

Q 
1
​
  Size: 64×128

初始化输出和状态: 在 SRAM 里腾出一块空地，用来存放准备累加的输出 O 
1
​
 （全填 0），以及用来做 Safe Softmax 的局部最大值向量 m 和局部除数向量 l。

O 
1
​
  Size: 64×128

🔸 内层循环 (Inner Loop): 遍历所有的 K 和 V 的分块
对于这 64 个 Query Token (Q 
1
​
 )，我们需要拿它们去跟所有的 4096 个 Key 进行点积，才能得到完整的注意力。所以我们要遍历全部 64 个 K,V 分块。

假设内层循环走到了第 1 个分块 K 
1
​
  和 V 
1
​
 ：

加载: 从 HBM 把 K 
1
​
  和 V 
1
​
  搬到 SRAM 里。

K 
1
​
  Size: 64×128

V 
1
​
  Size: 64×128

核心计算 1 (算 Attention Map 碎片): 我们让 Q 
1
​
  和 K 
1
​
  进行矩阵乘法。注意，K 
1
​
  需要转置。

公式: S 
1,1
​
 =Q 
1
​
 ×K 
1
T
​
 

Size 变化: (64×128)×(128×64)→64×64

💡 核心顿悟: 这个 64×64 的小矩阵 S 
1,1
​
  就是极其珍贵的注意力得分（Attention Map）。传统方法里它是恐怖的 4096×4096，但现在它极其迷你，完全塞在 SRAM 里！

核心计算 2 (Online Softmax 动态缩放):
在 SRAM 里，扫描这个 64×64 的 S 
1,1
​
 ，更新当前这一行的局部最大值 m，并算出局部的概率权重 P 
1,1
​
 。
同时，因为发现了新的最大值，立刻对旁边那个装着过去数据的 O 
1
​
  乘以一个极其简单的标量系数进行 Rescale。

P 
1,1
​
  Size: 依然是 64×64

O 
1
​
  Size: 依然是 64×128 （被 Rescale 了一下，维度不变）

核心计算 3 (加权求和，累加进 O):
把算好的概率权重 P 
1,1
​
  去乘以对应的 Value V 
1
​
 ，然后加到 O 
1
​
  里面。

公式: O 
1
​
 =O 
1
​
 +(P 
1,1
​
 ×V 
1
​
 )

Size 变化: (64×64)×(64×128)→64×128

💡 核心顿悟: 这个矩阵乘法的结果，它的形状完美契合 O 
1
​
 （64×128）。这意味着无论内层循环跑多少次，这个结果永远只会累加在这块固定大小的内存里，绝不会膨胀！

阅后即焚 (最关键的一步):
K 
1
​
 ,V 
1
​
 ,S 
1,1
​
 ,P 
1,1
​
  已经完成了它们的历史使命，直接从 SRAM 里彻底删除/覆盖，绝不写回 HBM。

🔸 内层循环继续：
拿 K 
2
​
 ,V 
2
​
  进 SRAM，重复步骤 2-5。
新算出来的 64×128 结果，继续累加到同一个 O 
1
​
  里面去。

🔹 内层循环结束，写回 HBM：
当 64 个 K,V 分块全部遍历完后，Q 
1
​
  已经看遍了天下所有的 Key 和 Value，此时 SRAM 里的 O 
1
​
  已经是绝对正确、最终的输出了。

最后动作: 把 O 
1
​
  从 SRAM 写回 HBM 的指定位置。

O 
1
​
  Size: 64×128

然后外层循环走向 Q 
2
​
 ，重复上述一切。

张量视角下的“内存魔法”总结
如果我们在整个前向传播过程中，站在 SRAM（极速缓存）门口当保安，查一下内存占用：

常驻居民: Q 
block
​
  (64×128) + O 
block
​
  (64×128)

流动人口: K 
block
​
  (64×128) + V 
block
​
  (64×128)

临时产生的计算碎片 (S 和 P): 64×64

你会发现，在任何一个时刻，SRAM 里最大的矩阵不过是 64×128 或 64×64 的规模。所有的累加操作（Reduction），都通过 [64 x 64] * [64 x 128] = [64 x 128] 这种维度收缩的矩阵乘法，被完美压缩进了一个固定大小的 O 
block
​
  里。

那个在标准 Attention 里毁天灭地的 4096×4096 庞然大物，从头到尾都没有在物理世界中完整存在过。

为了让你在脑海里能把这几个矩阵的相乘拼凑起来，我为你做了一个可视化的张量乘法维度推演控件，你可以直观地看到 [B 
r
​
 ×d] 和 [B 
c
​
 ×d] 是如何完美咬合的。
